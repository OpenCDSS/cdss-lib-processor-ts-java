# commands

The datastore does not have its own commands at this time.
The `ReadNrcsAwdb` command code in in the legacy SOAP code.
When the SOAP API support is removed, the commands can be moved here.
